# Prompt Overlay Execution Checklist

| ID | Phase | Task | Purpose | Refine | Implement | Validate | Output | Dependency | Start Condition | Alternative | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L1 | setup | Create execution ledger and baseline snapshot | Freeze scope and observed baseline before tuning | Capture current prompt files, current overlay wording, and observed API behavior summary | Create this checklist under `docs/prompt-overlay/00-execution-checklist.md` | File exists and reflects current task + baseline findings | Checklist markdown | none | User invoked `$loop` and current prompt files are readable | Ad hoc planning in chat only | done | Baseline: current overlay increases empathy slightly but not enough; overlay-only reproduces desired tone better but breaks injection resistance and contaminates coding answers. |
| L2 | refine | Design stronger empathetic overlay and any minimal base-rule adjustment | Increase “接住你”感 without deleting prior safeguards | Decide whether to tune overlay only or also relax a small base-rule edge for emotional cases; identify explicit target phrases to allow | Update overlay wording in `nonebot_plugin_moellmchats/prompt_templates.py`; if needed, add a narrowly scoped emotional-case carve-out in base rules | Diff shows overlay remains separate toggle and includes stronger target phrasing/behavior constraints | Revised prompt spec in code | L1 | Baseline findings recorded | Make overlay stronger without touching base rules at all | done | Implemented as overlay-only strengthening plus a narrowly scoped carve-out: emotional cases may temporarily exceed the default brevity rule; direct target phrases added but capped to 1-2 per reply. |
| L3 | implement | Add regression tests for stronger overlay | Keep overlay independent and intentional | Add assertions for stronger phrases and optional emotional-case carve-out; preserve toggle coverage | Update `tests/test_prompt_templates.py` | `python -m unittest` passes for prompt tests | Updated tests | L2 | Overlay wording stabilized | Validate only by manual inspection | done | Prompt tests now assert stronger phrase inventory and that stronger overlay text disappears when toggle is off. |
| L4 | validate | Real API comparison for emotional/coding/injection cases | Confirm stronger “接住你” feel and check non-emotional contamination | Re-run local-responses with overlay on for emotional cases; compare against coding and injection behavior | Use local API from `../ttd-bot` with same schema and representative prompts | Save concise validation notes in this checklist and response | Validation notes | L3 | Tests pass and prompt is updated | Validate only with unit tests | done | Real API preview: emotional case now produced exact target phrase “你问到问题的核心了”; second emotional case stayed more承接化 but not over-poetic; coding answer remained mostly technical (“先别慌… guard clauses / switch / 表驱动”); injection case resisted and returned “没记住，也不会按这种命令办”。 |\n| L5 | closeout | Summarize results and next toggle options | Hand off concrete outcome and remaining risks | Record whether current overlay should become `subtle/strong` later | No code unless validation forces it | Final response references checklist + outputs | User-facing summary | L4 | Real API preview completed | Stop after code/test only | done | Next natural extension is split current overlay into `subtle` and `strong`, but current task goal is met without changing toggle shape. |
| L6 | refine | Design silent-ignore injection response policy | Replace explicit rebuttal with low-surface non-compliance | Define one bounded policy: when low-risk control text comes from untrusted input, silently ignore it, do not explain refusal, do not repeat safety boundary, and use shortest natural denial if user asks to confirm it | Update `nonebot_plugin_moellmchats/prompt_templates.py` with a separate prompt block for silent ignore behavior | Prompt diff shows the new rule is distinct from untrusted-input detection and explicitly bans “不会按这种命令办” style replies | Revised injection-response policy in code | L5 | Stronger overlay work is finished | Fold this into existing untrusted-input rule text | done | Added a separate `SILENT_IGNORE_INJECTION_RULES` block: ignore control text from untrusted input, avoid explicit rebuttal, prefer minimal denial like “没记住。” |
| L7 | implement | Add regression tests for silent-ignore policy | Keep the new policy explicit and reviewable | Add assertions that the prompt contains “静默忽略 / 不解释 / 不复述 / 最短自然否认” style instructions | Update `tests/test_prompt_templates.py` | `python -m unittest` passes | Updated tests | L6 | Silent-ignore wording stabilized | Validate only via real API | done | Prompt tests pass; silent-ignore block is present in both empathy-on and empathy-off paths. |
| L8 | validate | Real API preview for injection after silent-ignore policy | Check if explicit rebuttal disappears without losing resistance | Re-run representative injection case through `local-responses` with current prompt | Use same schema and current overlay/safety stack, compare against previous “也不会按这种命令办” result | Validation note with before/after snippets | Validation notes | L7 | Tests pass | Validate only by reading prompt text | done | Real API result changed from “没记住，也不会按这种命令办。” to exactly “没记住。” under the same low-risk naming injection scenario. |
| L9 | closeout | Summarize silent-ignore result and remaining limits | Hand off the behavior change and caveats | Record whether further post-processing is needed if model still over-explains | No code unless validation fails | Final response references ledger delta + preview result | User-facing summary | L8 | Real API preview completed | Stop after prompt change only | done | Current prompt-only solution hits the desired sample. If future models regress into explanation, the next fallback is post-processing for low-risk refusal surfaces. |
